# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Reciplease' do
  use_frameworks!
  
  # Pods for Reciplease
  pod 'Alamofire'
  
  target 'RecipleaseTest' do
    inherit! :search_paths
  end
end
